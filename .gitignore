# Secrets — NEVER commit these
.env
.env.*
*.secret
secrets.yaml
secrets.json

# Claude Code local state — never commit these
.claude/
*.local.json

# Local tool directories
.tldr/
.tldrignore
.playwright-mcp/

# Local working notes / session handoffs — never committed
thoughts/

# Private planning doc — never committed
BACKLOG.md

# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Build artifacts
*.pyc
__pycache__/
node_modules/
dist/

# Logs
*.log

# internal notes for the claude-ha integration bridge — not shipped
.integration-bridge/

# Maestri multi-agent workspace (role dirs) — service dir, never shipped
.maestri/

# Team collaboration channel (requests/replies to Codex & the integration owner) — working correspondence, never shipped
.team-comm/
